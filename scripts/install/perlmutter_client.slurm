#!/bin/bash

# Base script generated by NERSC Batch Script Generator on https://iris.nersc.gov/utils/jobscript

#SBATCH -N 1
#SBATCH -C gpu
#SBATCH -G 1
#SBATCH -q regular
#SBATCH -A lcls_g
#SBATCH -t 1:0:0

# OpenMP settings:
export OMP_NUM_THREADS=1
export OMP_PLACES=threads
export OMP_PROC_BIND=spread

module load conda

# Install pytorch
# applications may perform better with --gpu-bind=none instead of --gpu-bind=single:1
srun -n 1 -c 128 --cpu_bind=cores -G 1 --gpu-bind=single:1 mamba create -n py3.11-nopsana-torch-rapids -c rapidsai -c conda-forge -c nvidia rapids=24.06 python=3.11 cuda-version=12.4 pytorch jupyterlab h5py